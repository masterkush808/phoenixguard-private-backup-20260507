# PhoenixGuard V3 default developer profile.
#
# Production live installs should use:
#   requirements/locks/live-win-py311.txt
#
# This default keeps `pip install -r requirements.txt` useful for development,
# but the real dependency authority lives in the split profiles under
# requirements/ and their compiled locks under requirements/locks/.
-r requirements/dev.in


